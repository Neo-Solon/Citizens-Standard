# DEPLOY CHECKLIST — Citizens Standard (2026-07-12)

**This zip mirrors the repo layout.** Root of the zip = root of `Neo-Solon/Citizens-Standard`.
You can drop it straight in. Full detail of every change: `HANDOFF_2026-07-12.md`.

```
index.html  declaration.html  how_the_engine_works.html  Citizens_Standard_Engine.html
papers.html  questions_and_answers.html  methodology.html  calibration_notes.html
CS_AI_Companion.{md,pdf,txt}   llms.txt
assets/          cs-banner.jpg (re-encoded), cs-badge-32/64.png
audio/           cs-overview.mp3  (NEW — 40:50)
papers/          14 papers, .docx + .pdf          papers.zip
full_paper_list/ 14 per-paper zips               full_paper_list_all.zip
replication/     11 packages + one-command harness   replication.zip (regenerated from it)
fx_exploratory_record/            two_circuit_supplementary_record/
```

---

## STEP 1 — Banner
`assets/cs-banner.jpg` is your new banner, re-encoded **1,731 KB → 424 KB** (JPEG q88, no visible loss).
`index.html` points at `assets/cs-banner.jpg?v=20260712b`.
- [ ] **Delete the stale `assets/cs-banner.png` and any old `cs-banner.jpg` from the repo.** One banner, one extension.

## STEP 2 — Site pages (8) + audio
- [ ] All 8 HTML pages replace the live versions.
- [ ] `audio/cs-overview.mp3` — new 40:50 cut. Referenced as `audio/cs-overview.mp3?v=20260712`.
      Bump the `?v=` string whenever you replace either asset, or browsers serve the cached copy.

## STEP 3 — Replication (the new part)
`replication/` now verifies everything in one command:

```bash
cd replication
python run_all.py
```

- The **11 paper packages keep their existing repo paths and are unchanged** —
  `replication/architecture_replication/`, etc. Each is still independently runnable exactly as before.
- The harness sits alongside them: `run_all.py`, `paper_pkg.py`, `packages/*/run.py` adapters.
- `replication.zip` is regenerated from `replication/`, so the folder and the zip cannot drift.
- [ ] `replication/CLAIM_BACKING_INDEX.md` is preserved (papers.html links to it).

Add `--report` for a **visual report** — `replication/report/REPORT.html`, a single page with the
pass/fail table, the exact values that failed to reproduce, and **all 57 figures regenerated by that
run** (not copied from the repo, so it cannot go stale). Full suite takes a few minutes; `--only <pkg>`
runs one in seconds.

**Expected result: `12/12 packages passed`.**

## STEP 4 — One stale value the harness caught (RESOLVED)
The first full harness run flagged `structural_buyer_replication`: its `all_results.txt` recorded
`psi* base=0.24`, while `verify_psi_plateau.py` produces `0.158`. One value out of 28.

Adjudicated against **Paper 8 itself**: the paper states ≈0.16 at the cohort-realistic dur≈40yr,
realized ψ*≈0.10, active float ≈0.90 — which is exactly what the script produces (0.158 / 0.098 /
0.902). `0.24` matches nothing in the paper. **The code was right; the golden file was stale**,
captured from an older revision and never refreshed.

`all_results.txt` has been regenerated. The other 27 values are unchanged. No paper text needs
editing — Paper 8 already carries the correct figures.

- [ ] Nothing to do. Expected result is now **`12/12 packages passed`**.

## STEP 5 — The download bundles
`full_paper_list_all.zip` ("Download all papers & data" on papers.html) has been **regenerated**.
It previously held only the 14 per-paper zips, so the flagship download could not actually be
verified. It now contains:

```
full_paper_list/                     14 per-paper zips (paper/ + data_package/)  — unchanged
replication/                         11 packages + the one-command verifier      — NEW
fx_exploratory_record/               exploratory: headline result is a NULL      — NEW
two_circuit_supplementary_record/    supplementary robustness                    — NEW
README.md                            how to verify; status of the two records    — NEW
```

Anyone who takes that one download can now run `cd replication && python run_all.py` and get a
pass/fail table. 27 MB.

- [ ] `replication.zip` (harness alone) is regenerated from `replication/` and still offered separately.
- [ ] `papers.zip` (PDFs only) unchanged.

**On the two exploratory records:** they are in the bundle for completeness, in clearly-named
folders whose READMEs state their status up front — the FX headline is a documented **null**. They
are NOT wired into `run_all.py`, because folding a falsification into the harness that verifies
published claims would miscategorise a null as support. They also remain at the repo root as the
canonical copies; the bundle copies are generated from them, so the two cannot drift.

## Post-push spot checks
- [ ] Engine loads on the "What you get" tab (not blank).
- [ ] Country selector drives BOTH the engine and the debt chart.
- [ ] Debt chart: US 246% by 2100 · Japan's dashed "if coupons never reprice" line · Türkiye's history sparkline.
- [ ] Mode Λ: all 5 pills move the chart.
- [ ] Hard-refresh index.html → new banner + new audio.
