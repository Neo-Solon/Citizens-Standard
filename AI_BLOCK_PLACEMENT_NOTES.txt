AI ANALYSIS FEATURE — deployment (pages now pre-built)
=======================================================

Everything is now drop-in. No HTML editing needed.

PUT THESE IN THE SITE ROOT (same folder as your other .html pages):

  1. index.html                    (REPLACES existing — now has the AI block at bottom)
  2. questions_and_answers.html    (REPLACES existing — AI block at bottom)
  3. papers.html                   (REPLACES existing — AI block above the papers list)
  4. CS_AI_Companion.md            (NEW — the consolidated companion the block links to)
  5. llms.txt                      (NEW — the AI-crawler convention file)

That's it. Overwrite the three existing pages with these versions, add the two
new files, push. The "Have an AI analyze this" block will appear on the
homepage, Q&A, and papers pages; llms.txt and the companion will resolve at:
  https://citizensstandard.org/llms.txt
  https://citizensstandard.org/CS_AI_Companion.md

NOT deployed here (leftover snippet, ignore): _ai_prompt_block.html was the raw
snippet; it's already baked into the three pages above, so you don't need it.

NOTHING costs anything. The visitor runs the prompt in their own AI. No API key,
no bill, no abuse surface.
